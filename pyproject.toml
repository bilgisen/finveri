[project]
name = "finveri"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "httpx",
    "pandas",
    "pandas-ta",
    "sqlalchemy",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
